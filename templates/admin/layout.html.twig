<!DOCTYPE html>
<html lang="ru">

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Международный визовый центр</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet">

    <!-- Include Editor style. -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/froala-editor/2.5.1/css/froala_editor.pkgd.min.css" rel="stylesheet" type="text/css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/froala-editor/2.5.1/css/froala_style.min.css" rel="stylesheet" type="text/css" />
    <link href="/css/custom.css" rel="stylesheet">

</head>

<body>

{% if app.user %}
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" target="_blank" href="/">МВЦ <i class="fa fa-home"></i></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                    aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    {% if app.user.hasRole('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_user_list') }}"><i class="fa fa-users"></i> Пользователи</a>
                        </li>
                    {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_setting_list') }}"><i class="fa fa-map"></i> Настройки</a>
                        </li>
                        {#<li class="nav-item">#}
                            {#<a class="nav-link" href="{{ path('admin_passport_photo_list') }}"><i class="fa fa-photo"></i> Люди с паспортами</a>#}
                        {#</li>#}
                        {#<li class="nav-item">#}
                            {#<a class="nav-link" href="{{ path('admin_quiz_type_list') }}"><i class="fa fa-map"></i> Типы вопросов</a>#}
                        {#</li>#}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_quiz_question_list') }}"><i class="fa fa-question"></i> Вопросы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_quiz_answer_list') }}"><i class="fa fa-question-circle"></i> Ответы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_region_list') }}"><i class="fa fa-map"></i> Регионы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_visa_list') }}"><i class="fa fa-cc-visa"></i> Визы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_map_list') }}"><i class="fa fa-map-marker"></i> Карта</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_geo_list') }}"><i class="fa fa-map"></i> Геолокации</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_article_list') }}"><i class="fa fa-file-text-o"></i> Статьи</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_fact_list') }}"><i class="fa fa-exclamation"></i> Факты</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_testimonial_list') }}"><i class="fa fa-comment"></i> Отзывы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_specialist_list') }}"><i class="fa fa-user-md"></i> Специалисты</a>
                        </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}"><i class="fa fa-user-circle"></i> ({{ app.user.username }}) Выйти</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endif %}

<!-- Page Content -->
<div class="container-fluid">
    <div class="row"> </div>
    <div class="row"> </div>
    <div class="row"> </div>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
            <strong></strong>{{ message }}
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
            <strong></strong>{{ message }}
        </div>
    {% endfor %}

    {% block content '' %}
</div>
</body>


<script src="/js/jquery-2.2.4.min.js"></script>
<script defer src="/js/popper.min.js"></script>
<script defer src="/js/bootstrap.min.js"></script>
<script defer src="https://cdnjs.cloudflare.com/ajax/libs/froala-editor/2.5.1//js/froala_editor.pkgd.min.js"></script>
<script defer src="/js/fontawesome.js"></script>
<script src="/js/app.js"></script>

{% block js '' %}

</html>