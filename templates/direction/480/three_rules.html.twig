<section id="direction-three-rules" class="mb-0 pt-5 pb-0 px-4">
    <div class="container justify-content-center text-center align-content-center ">
        <header class="text-center no-margin-bottom">
            <div class="h2 text-dark my-4">{{ title|raw }}</div>
            <div class="h4 text-dark my-4">{{ subtitle|raw }}</div>
        </header>
        <div class="text-left direction-three-rules-text text-gray">
            {{ region.text|raw }}
        </div>
    </div>
</section>

